<?xml version="1.0" encoding="UTF-8"?>
<pages:PopupPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             x:Class="CruiseBookingApp.Popups.PickerPopup"
             xmlns:animations="clr-namespace:Rg.Plugins.Popup.Animations;assembly=Rg.Plugins.Popup"
             xmlns:controls="clr-namespace:CruiseBookingApp.Controls"  
             xmlns:pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
             CloseWhenBackgroundIsClicked="true"
             Padding="16">
    <pages:PopupPage.Content>
        <StackLayout BackgroundColor="{StaticResource WhiteColor}" Padding="16">
            <Label Text="{Binding Title}" FontSize="{StaticResource LargeSize}" FontFamily="{StaticResource RobotoBold}"/>
            <ScrollView VerticalOptions="FillAndExpand">
                <StackLayout />
            </ScrollView>
            <controls:ButtonFrame Text="CANCEL" Padding="16" TextColor="{StaticResource AccentColor}" HorizontalOptions="End" />
        </StackLayout>
    </pages:PopupPage.Content>
</pages:PopupPage>
